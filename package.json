{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.3.3",
    "nuxt-icon": "^0.3.3"
  },
  "dependencies": {
    "nuxt-swiper": "^1.1.0",
    "vue-silentbox": "^3.0.4"
  }
}
